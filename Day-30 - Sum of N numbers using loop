#include <stdio.h>
int main() {
  long int n,i,sum;
  printf("Enter a number ");
  scanf("%ld", &n);
  sum = 0;
  for(i=0;i<=n;i++) {
    sum = (sum+i);
  }
  printf("\nSum of 1 to %ld is:  %ld", n, sum);
  return 0;
}
