#include <stdio.h>

int main(void) {
	int T;
	scanf("%d", &T);
	
	while(T--){
	    int X,Y;
	    scanf("%d %d", &X, &Y);
	    
	    (X%Y == 0)?printf("%d\n", X/Y):printf("%d\n", X/Y + X%Y);
	}
	return 0;
}
