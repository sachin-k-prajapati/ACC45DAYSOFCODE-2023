#include <stdio.h>

int main(void) {
    int T;
    scanf("%d", &T);
    
    while (T--) {
        int N;
        scanf("%d", &N);
        
        if (N >= 1 && N <= 10) {
            printf("Lower Double\n");
        } else if (N >= 11 && N <= 15) {
            printf("Lower Single\n");
        } else if (N >= 16 && N <= 25) {
            printf("Upper Double\n");
        } else {
            printf("Upper Single\n");
        }
    }
    
    return 0;
}
