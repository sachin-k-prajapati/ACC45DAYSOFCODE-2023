#include <stdio.h>

int main() {
    int T; // Number of test cases
    scanf("%d", &T);

    while (T--) {
        int X; // Battery level
        scanf("%d", &X);

        if (X <= 15) {
            printf("Yes\n");
        } else {
            printf("No\n");
        }
    }

    return 0;
}
