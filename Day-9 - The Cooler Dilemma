#include <stdio.h>

int main() {
    int T;
    scanf("%d", &T);

    while (T--) {
        int X, Y;
        scanf("%d %d", &X, &Y);

        int max_months = (X >= Y) ? 0 : Y / (X + 1);

        printf("%d\n", max_months);
    }

    return 0;
}
